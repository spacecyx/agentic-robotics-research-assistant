请先不要修改任何文件，只做 README 更新前的审计。

请在 ./agentic-robotics-research-assistant 项目中执行：
1. git status --short
2. git diff --stat
3. git diff --name-status
4. 如有 staged changes，检查 git diff --cached --stat 和 git diff --cached --name-status
5. 阅读当前 README.md

然后请输出：
1. 当前有哪些新增/修改文件
2. 每个新增/修改文件大概对应什么功能
3. 当前 README 哪些地方已经过时
4. README 应该新增哪些内容
5. 哪些内容不应该写进 README，因为代码中没有真实实现

限制：
- 不要修改任何文件
- 不要生成最终 README
- 只输出审计结果和更新建议
你现在只做代码审计，不要修改任何文件。

请检查当前项目中所有 LLM / chat model / invoke / generate 相关调用点，重点关注 LangGraph workflow 中的节点文件，例如 summarize、critique、generate_report 等节点。

请输出以下内容：

1. 当前项目中哪些文件、哪些函数直接调用了 LLM。
2. 每个调用点的输入是什么，输出是什么。
3. 当前是否已有 timeout、retry、exception handling、trace logging。
4. 哪些调用点最适合接入统一的 safe_llm_invoke 封装。
5. 给出一个最小改造计划，要求尽量少改现有代码结构，不影响已有 RAG 检索、评估、trace logging 和报告生成逻辑。

限制：
- 不要修改代码。
- 不要新增文件。
- 不要重构无关模块。
- 输出要具体到文件名和函数名。